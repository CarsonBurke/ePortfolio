import { Component, ReactNode } from "react";

export class About extends Component {
    render(): ReactNode {
        return (
            <main>
                
            </main>
        )
    }
}